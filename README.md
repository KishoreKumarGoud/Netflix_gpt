# netflix Gpt

-created scaffold using vite
tailwind css setup

# features
Login/sign-up page
   -sign-in,sign-up form
   -redirect to brpowse page
   form validation
   firebase setup
   -create useer signup account
   -connect firebase auth
   create user,sign in user
   store in RTK
   protected routes,naviagte based on the login status
   register TMDB,get Access Token,From TMDB docs get the API
   -main video with trailer frm tmdb using the key of video from YT
   create movies and trailer slice
   Main container with title and trailer bg video
   secondary container with category wise movies data fetching
   --write hooks for everything individually in movieslice

Browser Page(Only after Auth0)
   -Header
   -Main Movie
          --Trailer in bg,title and desc
          --Movie suggestions
              --Many movies
NetflixGPT
  -Search Bar
  -Movie suggestions

